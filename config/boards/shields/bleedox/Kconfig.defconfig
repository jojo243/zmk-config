if SHIELD_BLEEDOX_LEFT

config ZMK_KEYBOARD_NAME
	default "Bleedox"

config ZMK_SPLIT_ROLE_CENTRAL
	default y

endif

if SHIELD_BLEEDOX_LEFT || SHIELD_BLEEDOX_RIGHT

config ZMK_SPLIT
	default y

endif
